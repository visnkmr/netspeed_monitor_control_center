name: Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Download freewrap
      run: |
        wget https://sourceforge.net/projects/freewrap/files/freewrap/freeWrap%206.75/freewrap675.tar.gz/download -O ${{ github.workspace }}/freewrap.tar.gz
        tar -xzf ${{ github.workspace }}/freewrap.tar.gz
        echo $ls
        echo $(ls ${{ github.workspace }}/freewrap/linux64/)
        chmod +x ${{ github.workspace }}/freewrap/linux64/freewrap
        ${{ github.workspace }}/freewrap/linux64/freewrap gui.tcl -o gui
    - name: Get current date
      id: date
      run: echo "::set-output name=date::$(date +'%Y_%m_%dT%H_%M')"
    - name: Release
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CACHE_ON_FAILURE: true
      with:
        tag_name: ${{ steps.date.outputs.date }}
        name: ${{ steps.date.outputs.date }}
        draft: false
        prerelease: false
        files: |
          gui
        # file_name: perlink_amd64_windows
        body: |
          - ${{ github.event.head_commit.message }} ${{ github.sha }}